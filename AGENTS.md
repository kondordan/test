# AGENTS.md

## Project overview

Minimal Python stub repository (`kondordan/test`). The only tracked application file is `script.py`, which is currently empty. There are no package manifests, services, databases, or test suites.

## Development commands

| Action | Command |
|---|---|
| Run the script | `python3 script.py` |
| Syntax check | `python3 -m py_compile script.py` |

## Cursor Cloud specific instructions

- **Runtime:** Python 3 (system default, currently 3.12.x). No virtualenv or dependency installation is required.
- **Services:** None. There is no dev server, API, or database to start.
- **Lint / test:** No linter or test runner is configured in this repository.
- **Environment variables:** None required.
- **Gotchas:** The repository is a placeholder. If you expect a full application, confirm you are on the correct branch or repository before debugging "missing" setup files.
